#!/bin/bash

echo "Running quality pipeline before push..."
echo ""

npm run verify-codebase

if [ $? -ne 0 ]; then
    echo ""
    echo "Push BLOCKED — quality pipeline failed."
    echo "Fix all violations before pushing."
    echo ""
    exit 1
fi

echo ""
echo "Quality pipeline passed — pushing."
