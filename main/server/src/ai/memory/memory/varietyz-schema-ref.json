{
    "id": "varietyz-schema-ref",
    "type": "context",
    "priority": 25,
    "always_load": false,
    "triggers": ["schema", "db", "query", "plugin", "varietyz", "table", "clan db"],
    "depends_on": [],
    "placeholders": [],
    "content": "# Varietyz Schema Reference\n\nActive DBs for clan `varietyz`:\n\n## `clan` db (mode-independent)\n- `clan_chats` — every clan chat msg + broadcast (kind: PUBLIC|BROADCAST|SYSTEM)\n- `clan_eligibility_rules` — configurable eligibility rules (JSON blob)\n- `clan_invite_redemptions` — invite code redemption log\n- `clan_invites` — invite codes (role, expiry, use count, revoked)\n- `clan_member_history` — roster membership trail per account\n- `clan_members` — current roster snapshot\n- `clan_roster_diffs` — per-member diff events (rank changes, joins, leaves)\n- `clan_rosters` — full roster snapshots (fingerprinted, timestamped)\n- `clan_settings` — key/value clan config\n- `clan_snapshots` — rolling member count + online count observations\n- `clan_titles_current` — current configured rank titles\n- `clan_titles_history` — audit log of every title change\n\n## `plugin-seasonal-leagues-vi` db (only active mode — no plugin-main)\n\n### Tier 1 — current state (UPSERT)\n- `plugin_accounts` — account_hash, first_rsn, latest_rsn, account_type, first/last_seen\n- `plugin_bank` — has `slot` + `bank_tab` cols. bank_tab 1-9, 0=unfiled\n- `plugin_boosts` — skill boost deltas (diff = signed boost)\n- `plugin_clues` — clue scroll counts by tier (BEGINNER|EASY|MEDIUM|HARD|ELITE|MASTER)\n- `plugin_collection_log` — item, category, qty (category = widget section hierarchy)\n- `plugin_combat_achievements` — completed CA tasks: task_id, tier, points, completed_at\n- `plugin_current_state` — singleton per account. confirmed cols:\n  - `hitpoints`, `prayer`, `max_hitpoints`, `max_prayer` ✓\n  - `last_damage_dealt_target_kind` (NPC|PLAYER|ENVIRONMENT) ✓\n  - `last_damage_dealt_target_name` (NULL when target_kind=PLAYER) ✓\n  - `last_damage_taken_source_kind` (NPC|PLAYER|ENVIRONMENT) ✓\n  - `last_damage_taken_source_name` (NULL when source_kind=PLAYER) ✓\n  - NO `last_damage_taken_target_kind` col — strip from all queries\n  - `login_state` enum: UNKNOWN|STARTING|LOGIN_SCREEN|LOGIN_SCREEN_AUTHENTICATOR|LOGGING_IN|LOADING|LOGGED_IN|CONNECTION_LOST|HOPPING (no LOGGED_OUT)\n- `plugin_diaries` — achievement diary state: diary_id, diary_name, diary_region, tier, complete (1=done)\n- `plugin_equipment` — slot (HEAD|CAPE|AMULET|WEAPON|BODY|SHIELD|ARMS|LEGS|HAIR|GLOVES|BOOTS|JAW|RING|AMMO), item_id, item_name, qty, unit_price_gp\n- `plugin_farming` — patch state: patch_region_id, varbit_id, crop_id, crop_name, value, state (GROWING|HARVESTABLE|DISEASED|DEAD|EMPTY|...)\n- `plugin_inventory` — container_kind (MAIN|RUNE_POUCH), slot 0-27 (MAIN) / 0-5 (RUNE_POUCH), item, qty, unit_price_gp\n- `plugin_npc_kc` — kill count state per NPC: source_id, source_name, kc\n- `plugin_prayers` — prayer_id, prayer_name, active (1=on, 0=off)\n- `plugin_quests` — quest_id, quest_name, state (NOT_STARTED|IN_PROGRESS|COMPLETE)\n- `plugin_seed_vault` — seed vault contents: item_id, item_name, qty, unit_price_gp\n- `plugin_slayer` — has `wildy_tasks_completed`. count=remaining kc, count_original=kc at assignment\n- `plugin_stats` — skill, level, boosted, xp\n- `plugin_status_effects` — effect (POISONED|VENOMED|ANTIFIRE|OVERLOAD|STAMINA|...), active (1/0)\n\n### Tier 1 — connection/session state\n- `plugin_connection_status` — ws_connected, latency_ms, last_ping/pong_at, connected/disconnected_at\n- `plugin_sessions` — session_id, world, world_types (comma-sep: MEMBERS|LEAGUE|...), plugin_version, schema_version, connected/disconnected_at\n\n### Tier 1 — catalogs\n- `plugin_combat_achievement_catalog` — CA task definitions: task_id, task_name, description, tier, task_type, points, boss_id, boss_name\n- `plugin_items_catalog` — item_id, item_name, price_gp, last_seen_at\n\n### Tier 2 — event logs (append-only, carry dedup_hash)\n- `plugin_bank_changes` — qty_signed (+deposit/-withdraw)\n- `plugin_boosts_changes` — diff_before/diff_after per skill\n- `plugin_clues_changes` — count_before/count_after per tier\n- `plugin_collection_log_changes` — qty_signed, source_kind (boss|clue|pet|quest|random_drop|treasure_trail|other)\n- `plugin_combat_achievements_changes` — points_before/points_after\n- `plugin_deaths` — cause_kind (NPC|PLAYER|ENVIRONMENT), cause_name (NULL if PLAYER), hp_before, respawn coords\n- `plugin_deaths_lost_items` — death_id FK → plugin_deaths, item_id, item_name, qty, unit_price_gp\n- `plugin_diaries_changes` — tier_before/tier_after\n- `plugin_equipment_changes` — qty_signed (+equip/-unequip), unit_price_gp. NO `slot` col — strip from queries\n- `plugin_farming_changes` — state_before/state_after\n- `plugin_identity_drifts` — old_rsn, new_rsn (RSN change log)\n- `plugin_inventory_changes` — container_kind, qty_signed, cause_action/option/target. NO `slot` col — strip from queries\n- `plugin_login_state_transitions` — state_before/state_after (GameState enum)\n- `plugin_loot_drops` — item, qty, unit_price_gp, cause_kind, cause_name (NULL if PLAYER), kc (at time of loot)\n- `plugin_pet_drops` — pet_item_id, pet_item_name, trigger (boss_kill|skilling_event|clue_reward|...), source_kind/id/name\n- `plugin_prayers_changes` — qty_signed (+1 activate/-1 deactivate)\n- `plugin_quests_changes` — state_before/state_after\n- `plugin_seed_vault_changes` — qty_signed (+deposit/-withdraw)\n- `plugin_slayer_changes` — count_remaining_before/after\n- `plugin_stats_changes` — level_before/after, xp_before/after. source for xp/hr + level-up events\n- `plugin_status_effects_changes` — qty_signed (+1 activate/-1 clear)\n- `plugin_world_hops` — from_world, to_world\n\n### Tier 3 — bucketed aggregates\n- `plugin_damage_buckets` — source/target kind+id+name, hitsplat_type, minute_bucket, timestamp, dealt/taken totals + hit counts\n\nAll plugin queries: `{ db: 'plugin-seasonal-leagues-vi', clan: 'varietyz', sql: '...' }`\nAll clan queries: `{ db: 'clan', clan: 'varietyz', sql: '...' }`\n\nUse this file when querying varietyz data — no need to re-fetch db-schema."
}
