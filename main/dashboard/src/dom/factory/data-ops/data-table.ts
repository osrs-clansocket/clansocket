import { primitive } from "../core";

const dataTable = primitive("table", "data-table");
const thead = primitive("thead");
const tbody = primitive("tbody");
const tr = primitive("tr");
const th = primitive("th");
const td = primitive("td");

export { dataTable, thead, tbody, tr, th, td };
